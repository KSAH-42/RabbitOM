finir, l'implementation des headers rtsp
essayer de trouver une url rtsp pour h266 et implementer le décoder


// créer un nouveau repo pour l'onvif ou rajouter un nouveau projet dans la solution ?

rabbitom.onvif.web
rabbitom.onvif.web.deviceMgmt
rabbitom.onvif.web.io
rabbitom.onvif.web.ptz
rabbitom.onvif.web.events
rabbitom.onvif.web.media
rabbitom.onvif.clients
 // créer des clients distinct avec au moins les méthodes start/stop + event handlers sur le status de la comm -> ajouter une interface de base
 // ne pas exposer les dto des wsdl mais des business classes
 sealed class DeviceClient {}
 sealed class PtzClient {}
 sealed class IoClient {}
 sealed class MediaClient {}
 sealed class EventPullClient {}
 sealed class ImagingClient {}
 sealed class AnalyticClient {}
 sealed class AccessControlClient {}
rabbitom.onvif.clients.discovery
rabbitom.onvif.clients.events 









// The escape from thalezed, and it's nanar division. That's a serious compagny..... just take packages write code and build, that's it and push a globlins artifactory.

// thalezed C++ recorder with onvif integration = odm + wireshark + right click + copy and paste + send( socket , szText , 0 , strlen( szText ) ); // for a good compensation fixed at 70KEuro and for the java microservice used for send onvif ptz command, with a git clone...

// don't say it's shit, please respect it, thats thalezed systems;

//             
//   websocket        ws/jpeg+ff   rtp           rtp 
// web browser  <---> decoder.exe <--> proxy.exe <--> cam1
//                                                    /|\ rtp stream
//                              recorder.exe-----------+  
//                              live.kadafi.dll to handle rtsp

// or

//   websocket        ws/jpeg+ff   rtp           rtp 
// web browser  <---> decoder.exe <--> proxy.exe <--> cam1  <---- ptzservice.exe
//                                      /|\ rtp stream
//                        recorder.exe---+  
//                        live.kadafi.dll to handle rtsp
//                        onvif.dll (hardcoded string with socket including copyrights,and save securely by jfrog,...) don't laugth !

// the systems generate 3 streams for one device (2 for one 1 live, and 1 for recording) if we ignore wssocket.

// for receiving live software make rtsp request to proxy using kadafi.dll
// or ask to recorder but need to ask the archive with the start_time equals to DateTime.Now
