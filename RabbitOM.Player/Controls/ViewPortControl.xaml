<UserControl x:Class="RabbitOM.Player.Controls.ViewPortControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RabbitOM.Player.Controls"
             mc:Ignorable="d"
			 IsEnabledChanged="OnControlEnabledChanged"
             >

	<UserControl.Resources>
		<Storyboard x:Key="ZoomStoryboard">

			<DoubleAnimation Storyboard.TargetName="ImageScale"
							 Storyboard.TargetProperty="ScaleX"
							 Duration="0:0:0.1"
							 />

			<DoubleAnimation Storyboard.TargetName="ImageScale"
							 Storyboard.TargetProperty="ScaleY"
							 Duration="0:0:0.1"
							 />

			<DoubleAnimation Storyboard.TargetName="ImageTranslate"
							 Storyboard.TargetProperty="X"
							 Duration="0:0:0.1"
							 />

			<DoubleAnimation Storyboard.TargetName="ImageTranslate" 
							 Storyboard.TargetProperty="Y"
							 Duration="0:0:0.1"
							 />
		</Storyboard>
		
	</UserControl.Resources>
	

	<Grid ClipToBounds="True">

		<Image x:Name="ImageBox"
                x:FieldModifier="private"
                Margin="0,0,0,0" 
                SnapsToDevicePixels="True" 
                UseLayoutRounding="True" 
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
			    Stretch="{Binding StretchImage,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                >
			<Image.RenderTransform>
				<TransformGroup>
					<ScaleTransform x:Name="ImageScale" />
					<TranslateTransform x:Name="ImageTranslate" />
				</TransformGroup>
			</Image.RenderTransform>
		</Image>

		<Canvas Margin="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="Transparent"
                IsHitTestVisible="True"
				MouseDown="OnCanvasMouseLeftButtonDown"
                MouseUp="OnCanvasMouseUp"
                MouseMove="OnCanvasMouseMove"
                >
			
			<Rectangle x:Name="OuterRectangle"
                       Margin="0"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Stroke="WhiteSmoke" 
                       StrokeThickness="0.5"
                       StrokeDashArray="2 2"
                       Canvas.Left="{Binding ZoomProperties.SelectionX,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Canvas.Top="{Binding ZoomProperties.SelectionY,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Width="{Binding ZoomProperties.SelectionWidth,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Height="{Binding ZoomProperties.SelectionHeight,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Visibility="{Binding ZoomProperties.Visibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       />

			<Rectangle x:Name="InnerRectangle"
                       Margin="0"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Stroke="GreenYellow" 
                       StrokeThickness="1.0" 
                       Fill="#30AAA0A0" 
                       Visibility="{Binding ZoomProperties.Visibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       />

			<TextBlock x:Name="TextInfos"
                       Text="{Binding ZoomProperties.Text,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="{Binding ZoomProperties.TextColor,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Canvas.Left="{Binding ZoomProperties.TextPositionX,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Canvas.Top="{Binding ZoomProperties.TextPositionY,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Visibility="{Binding ZoomProperties.Visibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                       />

		</Canvas>

	</Grid>

</UserControl>
