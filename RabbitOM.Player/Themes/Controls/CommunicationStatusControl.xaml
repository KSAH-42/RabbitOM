<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:RabbitOM.Player.Controls"
                    >

    <Style TargetType="{x:Type local:CommunicationStatusControl}">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="Width" Value="auto" />
        <Setter Property="Height" Value="auto" />
        <Setter Property="ConnectingStatus" Value="False" />
        <Setter Property="AnimationSpeed" Value="2.0" />
        <Setter Property="Template">

            <Setter.Value>

                <ControlTemplate TargetType="{x:Type local:CommunicationStatusControl}">

                    <StackPanel Orientation="Vertical"
                                VerticalAlignment="Stretch"
                                HorizontalAlignment="Stretch"
                                >

                        <Rectangle x:Name="Part_Rectangle"
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               Height="15"
                               Width="15"
                               RenderTransformOrigin="0.5,0.5"
                               Fill="{TemplateBinding Fill}" 
                               >

                            <Rectangle.RenderTransform>
                                <RotateTransform x:Name="DiamondRotate" Angle="45" />
                            </Rectangle.RenderTransform>

                            <Rectangle.Triggers>

                                <EventTrigger RoutedEvent="Loaded">

                                    <BeginStoryboard>

                                        <Storyboard>

                                            <DoubleAnimation Storyboard.TargetName="DiamondRotate"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="45"
                                                         To="405"
                                                         Duration="{Binding Path=AnimationDuration, RelativeSource={RelativeSource TemplatedParent}}"
                                                         SpeedRatio="{Binding Path=AnimationSpeed, RelativeSource={RelativeSource TemplatedParent}}"
                                                         RepeatBehavior="Forever" 
                                                         />
                                        </Storyboard>

                                    </BeginStoryboard>

                                </EventTrigger>

                            </Rectangle.Triggers>

                        </Rectangle>

                        <TextBlock x:Name="Part_ConnectionMessageTextBlock"
                               HorizontalAlignment="Center"
                               FontSize="{TemplateBinding FontSize}" 
                               Foreground="{TemplateBinding Foreground}" 
                               Text="{TemplateBinding ConnectingMessage}"
                               />

                    </StackPanel>


                    <ControlTemplate.Triggers>

                        <Trigger Property="ConnectingStatus" Value="False">
                            <Setter TargetName="Part_ConnectionMessageTextBlock" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="Part_Rectangle" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>

        </Setter>

    </Style>

</ResourceDictionary>